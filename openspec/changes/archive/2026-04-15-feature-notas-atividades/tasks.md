# Tarefas: Feature de Lançamento de Notas

Estas são as tarefas necessárias para implementar a funcionalidade de lançamento de notas, com base no que foi explicitamente solicitado.

- [ ] **Backend (Xano)**:
    - [ ] 1. Criar a tabela `activity_grades` no banco de dados com os campos definidos no `design.md`.
    - [ ] 2. Criar o endpoint `POST /activity_grades` na API, implementando a lógica para registrar uma nova nota conforme especificado no `design.md`.
