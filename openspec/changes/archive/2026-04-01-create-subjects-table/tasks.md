## 1. Pre-flight Checks

- [x] 1.1 Create the `user` table as it was missing from the project.
- [x] 1.2 Confirm the `relation` in the `design.md` schema is correct (`user.id`).

## 2. Implementation

- [x] 2.1 Create the new table file `tables/subjects.xs` with the Xano schema defined in `design.md`.

## 3. Verification

- [x] 3.1 Confirm the `tables/subjects.xs` file has been created.
- [x] 3.2 Review the content of `subjects.xs` to ensure it matches the schema in `design.md`.
