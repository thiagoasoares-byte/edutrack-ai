# Tarefas: Implementação da Tabela de Disciplinas

- [x] Criar o arquivo `tables/subjects.xanoscript` com a definição da tabela `subjects`.
- [x] Adicionar os campos `id`, `created_at`, `name`, e `user_id` com os tipos corretos.
- [x] Adicionar o relacionamento entre `subjects.user_id` e `user.id`.
- [x] Executar o push das mudanças para o backend do Xano para criar a tabela.